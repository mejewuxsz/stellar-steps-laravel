# Long cache for static assets (images, fonts) – browsers reuse without revalidating
<IfModule mod_headers.c>
    <FilesMatch "\.(webp|png|jpg|jpeg|gif|ico|woff2?|ttf|eot)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
</IfModule>
